import React from 'react';
import './pet-list-item.css';

const PetListItem = () => {
    return (
        <div>
            there would be a pet cart
        </div>
    );
};

export default PetListItem;